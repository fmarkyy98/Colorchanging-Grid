#ifndef ENUMS_H
#define ENUMS_H

enum Color { Red, Yelow, Green, Blue };
enum Difficulty { Easy = 0, Normal = 10, Hard = 4 };
enum Size { Small = 4, Medium = 8, Big = 12 };

#endif // ENUMS_H
